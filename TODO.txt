  - Add a connect page to allow connecting if the UI is not served by a router?

GUI:
  - reget tree after any operation and animate changes
  - more testing
  - sort $default to top of tree level
  - show/maintain matrix of users/source|target addresses per vhost

Service:
  - Connect to router rather than listening for connections
  - Add ssl options for use when connecting to router

